---
title: "Solución Examen HTML 2026 - Star Wars: Young Jedi Adventures"
description: "Solución detallada paso a paso del examen de HTML de febrero de 2026"
---

import HtmlCodeSnippet from '../../../../components/HtmlCodeSnippet.astro';
import html2026ra2 from "../../../../codigo/ejercicios/html/examenes/examen_2026_febrero/ra2.html?raw";
import css2026ra2 from "../../../../codigo/ejercicios/html/examenes/examen_2026_febrero/ra2.css?raw";
import html2026ra3 from "../../../../codigo/ejercicios/html/examenes/examen_2026_febrero/ra3.html?raw";
import js2026ra3 from "../../../../codigo/ejercicios/html/examenes/examen_2026_febrero/ra3.js?raw";
import cartel2026 from "../../../../codigo/ejercicios/html/examenes/examen_2026_febrero/ficheros/cartel.jpg?url";
import logo2026 from "../../../../codigo/ejercicios/html/examenes/examen_2026_febrero/ficheros/Logo.png?url";
import logo1b2026 from "../../../../codigo/ejercicios/html/examenes/examen_2026_febrero/ficheros/Logo1.png?url";
import font2026 from "../../../../codigo/ejercicios/html/examenes/examen_2026_febrero/ficheros/Starwars.ttf?url";
import ResultImage from '../../../../components/ResultImage.astro';

# Solución Examen HTML - Febrero 2026

## RA2: Página Web Principal

Diseñar una página web sobre la serie **Star Wars: Young Jedi Adventures**.

### Características solicitadas:

- Fondo: Lightblue
- Fuente párrafos: Helvetica, 40% más grande
- Texto justificado
- CSS externo
- Imágenes: borde redondeado y sombras orientadas (DarkBlue)
- Imágenes con marco, texto las rodea
- Título: White con sombra Black
- Recuadro título: White, 8px, bordes redondeados
- Bloques Reparto y Premisa con logo atenuado de fondo
- Imagen del cartel: enlace a Wikipedia (ventana nueva)
- Uso de etiquetas semánticas

---

### Paso 1: Estructura HTML semántica

Creamos `ra2.html` utilizando exclusivamente **etiquetas semánticas** de HTML5:

- `<header>`: Contiene el título principal de la página.
- `<main>`: Agrupa el contenido principal de la página.
- `<section>`: Define las filas de contenido.
- `<article>`: Encierra cada bloque independiente de contenido.
- `<footer>`: Pie de página.

#### Jerarquía semántica:
```
<body>
  <header class="cabecera-principal">
    <h1>STAR WARS</h1>
    <h2>Las aventuras de los jóvenes Jedi</h2>
  </header>
  <main>
    <section class="fila-superior">
      <article class="columna-izquierda">...</article>
      <article class="columna-derecha">...</article>
    </section>
    <section class="fila-inferior">
      <article class="bloque-reparto">...</article>
      <article class="bloque-premisa">...</article>
    </section>
  </main>
  <footer>...</footer>
</body>
```

#### Elementos clave:
- Las imágenes utilizan `float: left` y `float: right` para que el texto las rodee.
- El cartel (`cartel.jpg`) es un enlace a Wikipedia que se abre en nueva pestaña.
- Las secciones Reparto y Premisa tienen un logo atenuado como fondo.

<HtmlCodeSnippet
  html={html2026ra2}
  css={css2026ra2}
  assets={{
    'Logo1.png': logo1b2026,
    'Logo.png': logo2026,
    'cartel.jpg': cartel2026,
    'Starwars.ttf': font2026,
  }}
/>

---

### Paso 2: CSS externo (ra2.css)

#### 2.1 Fuente personalizada

```css
@font-face {
    font-family: 'StarWars';
    src: url('Starwars.ttf') format('truetype');
}
```

#### 2.2 Configuración general

```css
body {
    background-color: lightblue;
    color: black;
    font-family: Helvetica, Arial, sans-serif;
    font-size: 140%; /* 40% más grande */
    margin: 0;
    padding: 20px;
}
```

- `font-family: Helvetica, Arial, sans-serif` → Fuente Helvetica con alternativas.
- `font-size: 140%` → 40% más grande que el tamaño base (100% + 40%).

#### 2.3 Cabecera

```css
.cabecera-principal {
    border: 8px solid white;
    border-radius: 20px;
    text-align: center;
    padding: 10px 30px;
    margin-bottom: 30px;
    max-width: 800px;
    margin-left: auto;
    margin-right: auto;
}

.cabecera-principal h1,
.cabecera-principal h2 {
    font-family: 'StarWars', sans-serif;
    color: white;
    text-shadow: 3px 3px 5px black;
    margin: 0;
}
```

- `border: 8px solid white` → Recuadro blanco grueso.
- `text-shadow: 3px 3px 5px black` → Sombra negra en el texto.

#### 2.4 Layout con Flexbox

```css
.fila-superior {
    display: flex;
    gap: 20px;
    margin-bottom: 20px;
    flex-wrap: wrap;
}

.columna-izquierda,
.columna-derecha {
    flex: 1;
    min-width: 300px;
    border: 2px solid rgba(255, 255, 255, 0.5);
    border-radius: 15px;
    padding: 15px;
    background-color: rgba(173, 216, 230, 0.3);
}
```

- `display: flex` crea una fila con dos columnas.
- `flex: 1` hace que ambas columnas ocupen el mismo espacio.
- `flex-wrap: wrap` permite que en pantallas pequeñas se apilen verticalmente.

#### 2.5 Imágenes flotantes con sombra

```css
.imagen-flotante {
    width: 150px;
    height: auto;
    border-radius: 15px;
    border: 2px solid darkblue;
    padding: 5px;
    background-color: white;
}

.imagen-flotante.izquierda {
    float: left;
    margin: 0 15px 10px 0;
    box-shadow: 5px 5px 10px darkblue;
}

.imagen-flotante.derecha {
    float: right;
    margin: 0 0 10px 15px;
    box-shadow: -5px 5px 10px darkblue;
}
```

- `float: left` / `float: right` → Hacen que el texto rodee las imágenes.
- `box-shadow: 5px 5px 10px darkblue` → Sombra hacia la derecha.
- `box-shadow: -5px 5px 10px darkblue` → Sombra hacia la izquierda.
- `overflow: hidden` en el contenedor evita que el float desborde.

#### 2.6 Fondo atenuado con logo

```css
.bloque-reparto::before,
.bloque-premisa::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 200px;
    height: 200px;
    background-image: url('Logo.png');
    background-repeat: no-repeat;
    background-position: center;
    background-size: contain;
    opacity: 0.15;
    pointer-events: none;
    z-index: 0;
}
```

- Se utiliza el **pseudo-elemento** `::before` para insertar el logo como fondo.
- `position: absolute` + `transform: translate(-50%, -50%)` centra el logo.
- `opacity: 0.15` lo atenúa al 15% de visibilidad.
- `pointer-events: none` evita que el logo interfiera con la interacción.
- `z-index: 0` mantiene el logo detrás del contenido (que tiene `z-index: 1`).

<HtmlCodeSnippet
  html={html2026ra2}
  css={css2026ra2}
  assets={{
    'Logo1.png': logo1b2026,
    'Logo.png': logo2026,
    'cartel.jpg': cartel2026,
    'Starwars.ttf': font2026,
  }}
/>

---

## Resultado Esperado RA2

<ResultImage src="/app/src/content/lmsgi/HTML/Examenes/images/examen_2026.png" alt="Resultado esperado del RA2 - 2026" />

---

## RA3: JavaScript - Cálculo del perímetro

### Características solicitadas:

- Al cargar la página: añadir párrafo "Cálculo del perímetro" en rojo y centrado.
- Dos `<input>` para introducir base y altura.
- Botón "Calcular perímetro".
- Al pulsar el botón: mostrar base, altura y perímetro en ventana emergente.
- Usar **únicamente funciones JavaScript en hoja externa (.js)**.
- Indicar resolución de pantalla óptima en un comentario.

---

### Paso 1: Estructura HTML (ra3.html)

```html
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Resolución de pantalla óptima: 1366x768 -->
    <title>RA3 - Cálculo del perímetro</title>
</head>
<body>
    <label for="base">Introduzca la base del rectángulo:</label>
    <input type="number" id="base" name="base">

    <br><br>

    <label for="altura">Introduzca la altura del rectángulo:</label>
    <input type="number" id="altura" name="altura">

    <br><br>

    <button id="calcular">Calcular el perímetro</button>

    <script src="ra3.js"></script>
</body>
</html>
```

#### Elementos clave:
- Los inputs tienen `type="number"` para asegurar que solo se introduzcan valores numéricos.
- El script se carga al **final del body** con `<script src="ra3.js"></script>` para garantizar que el DOM esté completamente cargado antes de que el JavaScript se ejecute (aunque también podemos usar `DOMContentLoaded`).
- El comentario HTML `<!-- Resolución de pantalla óptima: 1366x768 -->` cumple con el requisito del enunciado.

---

### Paso 2: JavaScript externo (ra3.js)

```javascript
/*
 * Resolución de pantalla óptima: 1366x768
 * Este archivo contiene las funciones JavaScript para calcular el perímetro de un rectángulo.
 * Se carga externamente desde ra3.html
 */

// Función que se ejecuta cuando la página ha cargado completamente
document.addEventListener('DOMContentLoaded', function() {
    // Crear el párrafo con el texto "Cálculo del perímetro" en rojo y centrado
    var parrafo = document.createElement('p');
    parrafo.textContent = 'Cálculo del perímetro';
    parrafo.style.color = 'red';
    parrafo.style.textAlign = 'center';
    parrafo.style.marginTop = '20px';
    
    // Insertar el párrafo al final del body
    document.body.appendChild(parrafo);
    
    // Asignar el evento click al botón
    document.getElementById('calcular').addEventListener('click', calcularPerimetro);
});

// Función que calcula y muestra el perímetro del rectángulo
function calcularPerimetro() {
    // Obtener los valores de los campos de entrada
    var base = parseFloat(document.getElementById('base').value);
    var altura = parseFloat(document.getElementById('altura').value);
    
    // Validar que los valores sean números válidos
    if (isNaN(base) || isNaN(altura) || base <= 0 || altura <= 0) {
        alert('Por favor, introduce valores numéricos válidos mayores que cero.');
        return;
    }
    
    // Calcular el perímetro: 2 * (base + altura)
    var perimetro = 2 * (base + altura);
    
    // Mostrar el resultado en una ventana emergente (alert)
    alert('El rectángulo con base: ' + base + ' y altura: ' + altura + ' tiene como perímetro: ' + perimetro);
}
```

#### Explicación paso a paso:

1. **Comentario inicial**: Indica la resolución óptima y describe el propósito del archivo.

2. **`DOMContentLoaded`**: Escucha el evento que indica que el DOM está completamente cargado. Esto asegura que los elementos existen antes de manipularlos.

3. **`document.createElement('p')`**: Crea dinámicamente un nuevo elemento `<p>`.

4. **Estilos inline**: Aplicamos `color: 'red'` y `textAlign: 'center'` directamente sobre el elemento creado.

5. **`document.body.appendChild(parrafo)`**: Inserta el párrafo al final del `<body>`.

6. **Evento click**: Asignamos la función `calcularPerimetro` al evento `click` del botón mediante `addEventListener`.

7. **`parseFloat()`**: Convierte el valor del input (que siempre es string) a número decimal.

8. **Validación**: Comprobamos que los valores sean números válidos y mayores que cero. Si no lo son, mostramos un mensaje de error.

9. **Fórmula**: `perimetro = 2 * (base + altura)`

10. **`alert()`**: Muestra el resultado en una ventana emergente con el formato solicitado.

<HtmlCodeSnippet
  html={html2026ra3}
/>

---

## Resultado Esperado RA3

<ResultImage src="/app/src/content/lmsgi/HTML/Examenes/images/examen_2026_2.png" alt="Resultado esperado del RA3 - 2026" />

---

## Archivos de la solución

### RA2

- `app/src/codigo/ejercicios/html/examenes/examen_2026_febrero/ficheros/ra2.html`
- `app/src/codigo/ejercicios/html/examenes/examen_2026_febrero/ficheros/ra2.css`

### RA3

- `app/src/codigo/ejercicios/html/examenes/examen_2026_febrero/ficheros/ra3.html`
- `app/src/codigo/ejercicios/html/examenes/examen_2026_febrero/ficheros/ra3.js`

### Recursos necesarios:

- `Logo1.png` → Logo para la columna superior izquierda
- `Logo.png` → Logo para el fondo atenuado de Reparto y Premisa
- `cartel.jpg` → Cartel de la serie (enlace a Wikipedia)
- `Starwars.ttf` → Fuente personalizada para el título

---

## Resumen de conceptos aplicados

### RA2 - HTML/CSS

| Concepto | Implementación |
|----------|---------------|
| Etiquetas semánticas | `<header>`, `<main>`, `<section>`, `<article>`, `<footer>` |
| CSS externo | `<link rel="stylesheet" href="ra2.css">` |
| Color de fondo | `background-color: lightblue` |
| Tamaño fuente | `font-size: 140%` (40% más grande) |
| Texto justificado | `text-align: justify` |
| Float | `float: left` / `float: right` |
| Flexbox | `display: flex` para columnas |
| Sombra caja | `box-shadow: 5px 5px 10px darkblue` |
| Sombra texto | `text-shadow: 3px 3px 5px black` |
| Pseudo-elemento | `::before` para fondo atenuado |
| Opacidad | `opacity: 0.15` |
| Z-index | Capas con `z-index` |
| Enlace externo | `target="_blank"` + `rel="noopener noreferrer"` |

### RA3 - JavaScript

| Concepto | Implementación |
|----------|---------------|
| JS externo | `<script src="ra3.js"></script>` |
| DOMContentLoaded | Esperar a que el DOM cargue |
| createElement | Crear elementos dinámicamente |
| appendChild | Insertar elementos en el DOM |
| getElementById | Seleccionar elementos |
| addEventListener | Asignar eventos |
| parseFloat | Convertir string a número |
| alert | Ventana emergente |
| Validación | `isNaN()` y comparaciones |
| Comentarios | `/* ... */` y `<!-- ... -->` |